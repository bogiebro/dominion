module Train where
import GameState

-- This should have the instances of NeuronUser. 
-- There should be one wrapper around a StateT,
-- and one wrapper around a ReaderT, for training
-- and running respectively.
